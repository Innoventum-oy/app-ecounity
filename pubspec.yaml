name: ecounity
description: "EcounityApp"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.8+17

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: ">=3.35.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  core: # for shared code from the core package
    git:
      url: git@github.com:Innoventum-oy/core.git
      path: core/
      ref: dynamic-objectmodel
  dio: ^5.9.2 # for making http requests
  flutter:
    sdk: flutter
  #internationalization:
  flutter_localizations:
    sdk: flutter
  html: ^0.15.4 # for parsing html
  intl: ^0.20.2 # for internationalization
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  package_info_plus: ^9.0.1 # for providing information about the application
  path_provider: ^2.0.8 # for getting the path of the application
  provider: ^6.0.0  # for state management
  shared_preferences: ^2.5.5  # for storing data locally
  url_launcher: ^6.3.1  # for launching urls
  another_flushbar: ^2.2.4 # for showing flushbars
  carousel_slider: ^5.0.0  # for image carousel with flexible aspect ratios
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9
  percent_indicator: ^4.2.3
  flutter_html: ^3.0.0-beta.2
  font_awesome_flutter: ^11.0.0
  email_validator: ^3.0.0
  intl_phone_number_input: ^0.7.4
  just_audio: ^0.10.5
  youtube_player_iframe: 6.0.2
  youtube_player_flutter: 10.0.1
  flutter_widget_from_html: ^0.17.2
  webview_flutter: ^4.8.0
  webview_flutter_android: ^4.10.15
  loader_overlay: ^5.0.0
  drag_drop_match_widget: ^0.0.1
  collection: ^1.19.0
  http: ^1.3.0
  crypto: any
dev_dependencies:
  archive: ^4.0.9
  build_runner: ^2.13.1
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_native_splash: ^2.1.6
  flutter_launcher_icons: ^0.14.1
  flutter_lints: ^6.0.0
  hive_ce_generator: ^1.10.0
  image: ^4.8.0
  test: ^1.25.2
  xml: ^6.6.1

dependency_overrides:

 core:
    path: /Users/jleinone/flutter_projects/core/core
    # local path for faster development

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true #for int
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Application assets
  assets:
    - assets/images/
    - assets/config/


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

#if you make changes, regenerate with flutter pub run flutter_native_splash:create
flutter_native_splash:
  android: true
  ios: true
  web: true
  color: "#E7FFDE"
  image: assets/images/ecounity-logo.png
  android_12:
    color: "#E7FFDE"
    image: assets/images/ecounity-logo.png

#if you make Android icon changes, regenerate with flutter pub run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/ecounity-logo.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/images/ecounity-logo.png"
scripts:
  gen_l10n: ./generate.sh
